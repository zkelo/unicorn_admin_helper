{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Скомпилировать и скопировать в папку с игрой",
            "type": "shell",
            "command": ".\\build.ps1",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
